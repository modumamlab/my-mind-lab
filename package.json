{
  "name": "my-mind-lab",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "netlify:dev": "netlify dev",
    "check": "node scripts/check-project.mjs",
    "check:admin": "node scripts/verify-admin-files.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^27.0.0",
    "vite": "^7.0.0"
  }
}
